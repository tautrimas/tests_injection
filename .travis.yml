language: php

php:
    - 5.4

before_install:

before_script:
    - mysql -e 'create database workshop_testing_test;'
    - echo "USE workshop_testing_test;\nUPDATE user SET password=PASSWORD('rootslaptazodis') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root
    - composer install

script:
    - vendor/bin/phpunit -c application --coverage-text

cache:
    directories:
        - vendor/
